<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.vuehouduan.mapper.MerchantMapper">

    <resultMap id="BaseResultMap" type="com.example.vuehouduan.entity.Merchant">
        <id column="id" property="id"/>
        <result column="shop_name" property="shopName"/>
        <result column="shop_address" property="shopAddress"/>
        <result column="shop_description" property="shopDescription"/>
        <result column="shop_logo" property="shopLogo"/>
        <result column="business_license" property="businessLicense"/>
        <result column="approval_status" property="approvalStatus"/>
        <result column="approve_time" property="approveTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, shop_name, shop_address, shop_description, shop_logo,
        business_license, approval_status, approve_time, create_time, update_time
    </sql>

    <select id="selectByApprovalStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM merchants WHERE approval_status = #{status}
    </select>

    <update id="updateApprovalStatus">
        UPDATE merchants SET approval_status = #{status}, approve_time = NOW() WHERE id = #{id}
    </update>

    <update id="updateShopInfo">
        UPDATE merchants SET shop_name = #{shopName}, shop_address = #{shopAddress},
        shop_description = #{shopDescription} WHERE id = #{id}
    </update>

    <update id="updateShopLogo">
        UPDATE merchants SET shop_logo = #{shopLogo} WHERE id = #{id}
    </update>

    <select id="countByApprovalStatus" resultType="int">
        SELECT COUNT(*) FROM merchants WHERE approval_status = #{status}
    </select>

    <update id="updateMerchantSelective">
        UPDATE merchants
        <set>
            <if test="shopName != null">shop_name = #{shopName},</if>
            <if test="shopAddress != null">shop_address = #{shopAddress},</if>
            <if test="shopDescription != null">shop_description = #{shopDescription},</if>
            <if test="shopLogo != null">shop_logo = #{shopLogo},</if>
            <if test="businessLicense != null">business_license = #{businessLicense},</if>
            <if test="approvalStatus != null">approval_status = #{approvalStatus},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

</mapper>