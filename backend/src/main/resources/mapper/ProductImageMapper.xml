<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.vuehouduan.mapper.ProductImageMapper">

    <resultMap id="BaseResultMap" type="com.example.vuehouduan.entity.ProductImage">
        <id column="id" property="id"/>
        <result column="product_id" property="productId"/>
        <result column="image_url" property="imageUrl"/>
        <result column="is_main" property="isMain"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, product_id, image_url, is_main, sort_order, create_time
    </sql>

    <select id="selectByProductId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM product_images
        WHERE product_id = #{productId} ORDER BY sort_order ASC
    </select>

    <select id="selectMainByProductId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM product_images
        WHERE product_id = #{productId} AND is_main = 1 LIMIT 1
    </select>

    <update id="clearMainImage">
        UPDATE product_images SET is_main = 0 WHERE product_id = #{productId}
    </update>

    <update id="setMainImage">
        UPDATE product_images SET is_main = 1 WHERE id = #{id}
    </update>

    <delete id="deleteByProductId">
        DELETE FROM product_images WHERE product_id = #{productId}
    </delete>

    <insert id="insertBatch">
        INSERT INTO product_images (product_id, image_url, is_main, sort_order, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.productId}, #{item.imageUrl}, #{item.isMain}, #{item.sortOrder}, NOW())
        </foreach>
    </insert>

    <select id="countByProductId" resultType="int">
        SELECT COUNT(*) FROM product_images WHERE product_id = #{productId}
    </select>

</mapper>