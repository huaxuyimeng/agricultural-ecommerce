<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.vuehouduan.mapper.FavoriteMapper">

    <resultMap id="BaseResultMap" type="com.example.vuehouduan.entity.Favorite">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="type" property="type"/>
        <result column="target_id" property="targetId"/>
        <result column="target_name" property="targetName"/>
        <result column="target_image" property="targetImage"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, type, target_id, target_name, target_image, create_time
    </sql>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM favorites WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="selectByUserIdAndType" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM favorites
        WHERE user_id = #{userId} AND type = #{type} ORDER BY create_time DESC
    </select>

    <select id="selectByUserIdAndTargetId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM favorites
        WHERE user_id = #{userId} AND type = #{type} AND target_id = #{targetId}
    </select>

    <delete id="deleteByUserIdAndTargetId">
        DELETE FROM favorites WHERE user_id = #{userId} AND type = #{type} AND target_id = #{targetId}
    </delete>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM favorites WHERE user_id = #{userId}
    </select>

    <select id="countByUserIdAndType" resultType="int">
        SELECT COUNT(*) FROM favorites WHERE user_id = #{userId} AND type = #{type}
    </select>

    <select id="countByProductId" resultType="int">
        SELECT COUNT(*) FROM favorites WHERE target_id = #{targetId} AND type = 'product'
    </select>

    <select id="existsByUserIdAndProductId" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM favorites WHERE user_id = #{userId} AND type = 'product' AND target_id = #{targetId})
    </select>

    <delete id="deleteByUserId">
        DELETE FROM favorites WHERE user_id = #{userId}
    </delete>

</mapper>