<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.vuehouduan.mapper.LoginHistoryMapper">

    <resultMap id="BaseResultMap" type="com.example.vuehouduan.entity.LoginHistory">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="ip_address" property="ipAddress"/>
        <result column="user_agent" property="userAgent"/>
        <result column="login_time" property="loginTime"/>
        <result column="logout_time" property="logoutTime"/>
        <result column="duration" property="duration"/>
        <result column="login_status" property="loginStatus"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, username, ip_address, user_agent, login_time, logout_time, duration, login_status
    </sql>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM login_history
        WHERE user_id = #{userId} ORDER BY login_time DESC
    </select>

    <select id="selectLatestByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM login_history
        WHERE user_id = #{userId} ORDER BY login_time DESC LIMIT 1
    </select>

    <update id="recordLogout">
        UPDATE login_history SET logout_time = NOW(),
        duration = TIMESTAMPDIFF(SECOND, login_time, NOW()) WHERE id = #{id}
    </update>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM login_history WHERE user_id = #{userId}
    </select>

    <select id="countByStatus" resultType="int">
        SELECT COUNT(*) FROM login_history WHERE login_status = #{status}
    </select>

    <delete id="deleteBeforeDate">
        DELETE FROM login_history WHERE login_time &lt; #{beforeDate}
    </delete>

    <select id="selectByCondition" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM login_history
        <where>
            <if test="userId != null">AND user_id = #{userId}</if>
            <if test="username != null and username != ''">AND username = #{username}</if>
            <if test="loginStatus != null and loginStatus != ''">AND login_status = #{loginStatus}</if>
            <if test="startTime != null">AND login_time &gt;= #{startTime}</if>
            <if test="endTime != null">AND login_time &lt;= #{endTime}</if>
        </where>
        ORDER BY login_time DESC
    </select>

</mapper>